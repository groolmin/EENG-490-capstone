# Python Package for Capstone Project

## Prerequisites:

 1. Jetson.GPIO python package
  - pip3 install Jetson.GPIO
  - sudo groupadd -f -r gpio
  - sudo usermod -a -G gpio {input linux machine's username here} 
